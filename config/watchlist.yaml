# T1 watchlist (ARCHITECTURE.md §6) — ~20 underlyings/segment, 60s cadence,
# full chain (2 expiries x ~40 strikes x 2). Symbols are matched against
# `instruments.symbol` after `kairodex ingest sync-instruments` has run for
# that market — this file only lists tickers, not vendor keys.
#
# This is a proposed default (liquid F&O / most-traded names), not a
# strategy decision — edit freely before `kairodex ingest sync-watchlist`.
#
# Verified against a live sync-instruments pull on the VM, 2026-08-04:
# TATAMOTORS is no longer a listed NSE symbol as of this session's live
# instrument master (superseded by its 2025 demerger); Bank Nifty's real
# trading_symbol is "BANKNIFTY", not "Nifty Bank".

nse_index:
  - "Nifty 50"
  - BANKNIFTY

nse_stock:
  - RELIANCE
  - HDFCBANK
  - ICICIBANK
  - INFY
  - TCS
  - SBIN
  - AXISBANK
  - KOTAKBANK
  - LT
  - BHARTIARTL
  - ITC
  - HINDUNILVR
  - BAJFINANCE
  - MARUTI
  - M&M
  - SUNPHARMA
  - TATASTEEL
  - ADANIENT
  - WIPRO
  - ONGC

# EMPTY, deliberately: verified live (2026-08-04) that LSE's entire options
# catalog (3,186 underlyings) carries no SPX/NDX/RUT — equity/ETF options
# only (SPY/QQQ/IWM are the closest listed proxies, already in us_stock).
# `list_expiries()` for all three returns zero contracts, no error, just an
# empty vendor catalog. This isn't a watchlist typo — it's a vendor-coverage
# gap for the whole US_INDEX segment. See docs/PROGRESS.md for the decision.
us_index: []

us_stock:
  - AAPL
  - MSFT
  - NVDA
  - AMZN
  - GOOGL
  - META
  - TSLA
  - AMD
  - NFLX
  - JPM
  - SPY
  - QQQ
  - XOM
  - BAC
  - WMT
  - DIS
  - INTC
  - CRM
  - ORCL
  - COST
