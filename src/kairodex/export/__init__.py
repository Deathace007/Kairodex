"""Export bundle builder + research-notes import — ARCHITECTURE.md §14's
"Claude Code loop": `kairodex export` writes a self-describing bundle out,
`kairodex research import-notes` reads findings back in.
"""
